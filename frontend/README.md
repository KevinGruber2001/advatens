# frontend

React + Vite dashboard for Advatens. See the [root README](../README.md).

- `npm run dev` — dev server (needs `.env`, copy `.env.example`)
- `npm run dev:mock` — dev server with MSW mocks, no backend needed
- `npm run prebuild` — regenerate the API client from `../openapi.yml`
- `npm run build` — codegen + typecheck + production build
