# Only needed when running the frontend standalone (npm run dev).
# When using docker compose, these come from the root .env instead.
VITE_API_BASE_URL=http://localhost:8888
VITE_CLERK_PUBLISHABLE_KEY=
